class DoctorModel {
  String? image, name, experience;
  num? rate, pay;

  DoctorModel(
      {required this.image,
      required this.experience,
      required this.name,
      required this.pay,
      required this.rate});
}
